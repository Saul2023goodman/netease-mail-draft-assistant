{
  "name": "netease-mail-draft-assistant",
  "version": "5.0.0",
  "private": true,
  "description": "Chrome MV3 NetEase 163 standalone outreach task workbench",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
